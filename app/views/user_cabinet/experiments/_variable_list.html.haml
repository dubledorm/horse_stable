.h4
  = t('.title')
.row
  %hr
  #experiment_variable_list
    - tabs_array = []
    - @resource.variables_sets.sets&.each do |set_of_variables|
      - tabs_array << { tabId: set_of_variables.set_id.to_s , tabName: set_of_variables.human_set_name }
    - tabs_array << { tabId: 'experiment_new_variable_set', tabName: '+' }
    -# = react_component('experiments/TabSelectorPanel', { fieldObjects: tabs_array, activeTabId: tabs_array.first[:tabId] })

  #experiment_new_variable_set
    -#{ tabs_array.count > 1 ? {class: :hidden} : {} }
    = react_component('experiments/SetOfVariablesEditForm', variable_editor_options(nil))
    -# = form_with(model: [@resource, Variables::SetOfVariables], local: true, url: add_set_of_variable_user_cabinet_experiment_path(id: @resource.id), method: :post) do |f|
    -#   .form-group.required
    -#     = f.label(Variables::SetOfVariables.human_attribute_name(:human_set_name))
    -#     = f.text_field :new_variable_set_name
    -#   .form-group
    -#     %button.btn.btn-primary.btn-lg{:name => "submit", :required => "required", :type => "submit"}
    -#       = I18n.t('forms.article_new.create')

