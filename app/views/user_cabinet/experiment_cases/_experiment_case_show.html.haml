= rc_string_field(@resource, :number, user_cabinet_experiment_experiment_case_path(id: @resource.id, experiment_id: params[:experiment_id]), @read_only)
= rc_string_field(@resource, :human_name, user_cabinet_experiment_experiment_case_path(id: @resource.id, experiment_id: params[:experiment_id]), @read_only)
= rc_text_field(@resource, :human_description, user_cabinet_experiment_experiment_case_path(id: @resource.id, experiment_id: params[:experiment_id]), @read_only)
%h2
  = Operation.model_name.human(count: 3)

.type_operations_block
  %h4
    = t('.check')
  = render('operation_title')
  - @resource.operations.check.each do |operation|
    .operation
      = render('operation_show', locals: { operation: operation.decorate })
  %div
    = render('add_operation_button', locals: { operation_type: 'check' })
.type_operations_block
  %h4
    = t('.do')
  = render('operation_title')
  - @resource.operations.do.each do |operation|
    .operation
      = render('operation_show', locals: { operation: operation.decorate })
  %div
    = render('add_operation_button', locals: { operation_type: 'do' })
.type_operations_block
  %h4
    = t('.next')
  = render('operation_title')
  - @resource.operations.next.each do |operation|
    .operation
      = render('operation_show', locals: { operation: operation.decorate })
  %div
    = render('add_operation_button', locals: { operation_type: 'next' })
